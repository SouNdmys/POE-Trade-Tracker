[package]
name = "ptt-ocr-win"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Windows.Media.Ocr adapter for POE Trade Tracker BGRA captures"

[dependencies]
ptt-core = { path = "../ptt-core" }
ptt-vision = { path = "../ptt-vision" }

[target.'cfg(windows)'.dependencies]
windows = { version = "0.61.3", features = [
    "Foundation",
    "Globalization",
    "Graphics_Imaging",
    "Media_Ocr",
    "Win32_Foundation",
    "Win32_System_WinRT",
    "Win32_System_Threading",
] }

[[bin]]
name = "winrt-ocr-probe"
path = "src/bin/winrt_ocr_probe.rs"
